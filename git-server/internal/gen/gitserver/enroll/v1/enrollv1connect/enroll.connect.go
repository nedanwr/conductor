// Enrollment service contract. Boundary source of truth.
//
// The service trust domain's bootstrap door: a joining node presents a
// pre-shared bootstrap secret and a certificate-signing request, and the
// cluster trust anchor returns a short-lived signed identity plus the root to
// trust. This is the one path consulted at enrollment (and rotation); steady
// state peer-to-peer auth is direct mTLS against the returned root and never
// touches this service. It is kept logically separate from the placement
// directory so "the root of all trust" and "where everything is" are not one
// compromise surface.

// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gitserver/enroll/v1/enroll.proto

package enrollv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/nedanwr/conductor/git-server/internal/gen/gitserver/enroll/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EnrollServiceName is the fully-qualified name of the EnrollService service.
	EnrollServiceName = "gitserver.enroll.v1.EnrollService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EnrollServiceEnrollProcedure is the fully-qualified name of the EnrollService's Enroll RPC.
	EnrollServiceEnrollProcedure = "/gitserver.enroll.v1.EnrollService/Enroll"
)

// EnrollServiceClient is a client for the gitserver.enroll.v1.EnrollService service.
type EnrollServiceClient interface {
	Enroll(context.Context, *connect.Request[v1.EnrollRequest]) (*connect.Response[v1.EnrollResponse], error)
}

// NewEnrollServiceClient constructs a client for the gitserver.enroll.v1.EnrollService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEnrollServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EnrollServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	enrollServiceMethods := v1.File_gitserver_enroll_v1_enroll_proto.Services().ByName("EnrollService").Methods()
	return &enrollServiceClient{
		enroll: connect.NewClient[v1.EnrollRequest, v1.EnrollResponse](
			httpClient,
			baseURL+EnrollServiceEnrollProcedure,
			connect.WithSchema(enrollServiceMethods.ByName("Enroll")),
			connect.WithClientOptions(opts...),
		),
	}
}

// enrollServiceClient implements EnrollServiceClient.
type enrollServiceClient struct {
	enroll *connect.Client[v1.EnrollRequest, v1.EnrollResponse]
}

// Enroll calls gitserver.enroll.v1.EnrollService.Enroll.
func (c *enrollServiceClient) Enroll(ctx context.Context, req *connect.Request[v1.EnrollRequest]) (*connect.Response[v1.EnrollResponse], error) {
	return c.enroll.CallUnary(ctx, req)
}

// EnrollServiceHandler is an implementation of the gitserver.enroll.v1.EnrollService service.
type EnrollServiceHandler interface {
	Enroll(context.Context, *connect.Request[v1.EnrollRequest]) (*connect.Response[v1.EnrollResponse], error)
}

// NewEnrollServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEnrollServiceHandler(svc EnrollServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	enrollServiceMethods := v1.File_gitserver_enroll_v1_enroll_proto.Services().ByName("EnrollService").Methods()
	enrollServiceEnrollHandler := connect.NewUnaryHandler(
		EnrollServiceEnrollProcedure,
		svc.Enroll,
		connect.WithSchema(enrollServiceMethods.ByName("Enroll")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gitserver.enroll.v1.EnrollService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EnrollServiceEnrollProcedure:
			enrollServiceEnrollHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEnrollServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEnrollServiceHandler struct{}

func (UnimplementedEnrollServiceHandler) Enroll(context.Context, *connect.Request[v1.EnrollRequest]) (*connect.Response[v1.EnrollResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gitserver.enroll.v1.EnrollService.Enroll is not implemented"))
}
